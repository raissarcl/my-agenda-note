import { StyleSheet } from 'react-native';

export const notesListHeaderStyles = StyleSheet.create({
  actions: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    paddingRight: 4,
  },
});
